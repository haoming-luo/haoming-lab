# Online presentation project

This repository is ONLY the public frontend Lab website. Do not copy backend code, training code, model weights, experiment logs, credentials, or unpublished research here.

Preserve the existing structural-design UI unless requested. Recorded results must not be represented as newly computed inference. Keep the small demonstration note and documented matching behavior.

Use relative paths compatible with /haoming-lab/ and a future custom-domain root. Do not change the separate personal homepage. Deploy only public/ via the Pages workflow. Run node tests/static-demo.mjs and node build.mjs before publishing.
